# MAA Codex 工作约定

## 仓库概览

- `src/MaaCore/` 是 C++20 核心，负责控制器、任务编排、识别与状态管理。
- `src/MaaWpfGui/` 是 Windows WPF 客户端，采用 C#、XAML 与 MVVM 分层。
- `resource/` 保存任务流水线、模板、OCR 与各服务器差异资源。
- `include/` 是公开 C API，其他 `src/*` 多为语言绑定、工具或独立客户端。
- 跨模块改动或定位入口时按需阅读 `.codex/docs/architecture.md`，不要默认把详细知识全部载入上下文。

## 指令与修改原则

- 修改文件前先读取作用域内最近的 `AGENTS.md`；更深层规则优先于本文件。
- `3rdparty/EmulatorExtras/`、`src/MaaUtils/`、`src/MaaMacGui/`、`src/MAAUnified/`、`src/maa-cli/` 和 `test/` 是 Git 子模块，开发时默认只读；优先在父仓库完成适配，不修改子模块内容或 gitlink。只有用户明确要求涉及子模块时，才读取其自身 `AGENTS.md` 并单独评估修改。
- 先定位既有模式和测试，再做最小范围修改；不要顺手重构无关代码或覆盖用户改动。
- 验证应与风险相称；格式化、静态检查、程序检查和 `Debug x64` 构建可按需执行，运行测试前必须获得用户明确授权。
- TOML/YAML 键、枚举值、模型 ID、命令、路径、类型名和协议字段保留原始英文，无需把整份 Codex 配置改成英文。
- 个人配置文档不得记录用户名、盘符或本机绝对路径；使用 `$CODEX_HOME`、`<repo-root>` 等稳定占位符。
- Markdown 段落不要手动强制换行，不使用行尾双空格或为排版插入无意义的 `<br>`。

## 知识文档维护

- `.codex/docs/` 只追踪程序主体功能、主要界面、核心接口和稳定架构的变化，并提炼可复用的代码、模块与接口供后续开发定位。
- 知识文档描述当前长期有效的结构与契约，不记录版本更新流水、单次修复过程、某个版本的参数细节或短期活动功能。
- 功能变化只有影响长期入口、跨层边界、公共契约或复用方式时才更新知识文档；普通子功能沿所属主体功能说明，不因近期新增而单独提升为架构章节。

## 开发检查与现场测试

- 开发时如需查看游戏界面或游戏数据，优先连接本地模拟器直接检查，不主动联网搜索。只有用户明确要求联网查询，或用户提供了具体网站、网页等信息时，才使用网络来源。
- 本地模拟器未启动或不可用时，先向用户申请启动模拟器或授权启动，不自行扩大设备操作范围。
- 编译本地程序统一使用 `Debug x64` 配置；允许进行不控制模拟器的本地程序检查。
- 不建议使用 Python 等脚本间接调用 MAA 控制模拟器。现场测试优先启动 MAA 程序并由 MAA 控制模拟器；任何测试都必须先获得用户明确授权。

## Git 分支与 PR 隔离

- 日常开发以最新 `upstream/dev-v2` 为唯一基线；`origin/dev-v2` 只通过 GitHub 网页 **Sync fork** 保持同 SHA 镜像。本地 `dev-v2` 非必需；若存在，只允许作为无个人提交的临时镜像。
- `codex/dev` 只保存个人 AI 配置、`AGENTS.md`、`CLAUDE.md`、skills 和知识文档，不推送到上游，也不合并或变基到 PR 分支。
- 常规更新 `codex/dev` 时只允许普通 `rebase upstream/dev-v2`。用户明确指定 Release、tag 或 commit 时，可以进入“指定基线维护”模式，但必须验证目标提交与 `upstream/dev-v2` 的祖先关系，并在用户提供 tag 时验证 tag 指向，再使用普通 rebase 语义重放个人提交；禁止 merge 上游、禁止 `git pull` 生成 merge commit。
- 新功能开发前确认 `codex/dev` 工作区干净，再手动创建并切换到 `wip/<topic>`；产品改动只提交在该 WIP 分支。
- `wip/*` 默认保留在本地；仅在用户明确授权并完成推送前检查后，才可推送到个人仓库 `origin` 的同名分支，禁止推送到官方主仓库 `upstream`。
- WIP 开发期间默认不移动基线；用户明确要求根据更新后的 `codex/dev` 重接 WIP 时，必须记录旧 WIP 基线与远端 SHA，仅重放产品提交，使用 `range-diff` 验证补丁等价，并在改写已推送历史前获得精确 lease 授权。
- 删除带未提交改动的 WIP 前必须明确确认保留或丢弃；不得自动 stash、reset、clean 或删除文件。
- 工作提交只包含产品代码及其必要测试；个人配置若需调整，必须独立提交在 `codex/dev`。
- 准备 PR 时，从最新 `upstream/dev-v2` 新建 `feat/<topic>`，仅 cherry-pick 已审查的产品提交，禁止把 `codex/dev` 或 `wip/*` 整体合并进去；`AGENTS.md`、`CLAUDE.md`、`.agents/**`、`.claude/**` 和 `.codex/**` 等个人 AI 配置不得进入 PR。
- 暂存必须列出明确路径，不使用 `git add .` 或 `git add -A`；提交前检查 staged diff，推送前检查相对 `upstream/dev-v2` 的完整提交范围。
- 提交前先总结工作区全部修改，按可独立审查的修改主题拆分提交；每个提交只包含一个连贯意图，不把无关修改一次性全部纳入同一提交。
- 提交摘要建议使用 `<type>(<scope>): <description>`。默认省略 `<scope>`、仅保留摘要且不写正文，即 `<type>: <description>`；只有用户明确要求时才添加 `<scope>` 或正文中的具体说明。
- commit message 正文不要按固定列宽手动强制换行；只在自然段、列表项或语义边界处换行。
- `.git/info/exclude` 只用于本机未跟踪文件的可见性，不是安全边界；不要自动改写它，也不要依赖它阻止已跟踪或强制暂存的文件进入提交。
- 提交、推送、改写历史或绕过检查前必须说明范围并获得用户明确授权；禁止未经授权使用 `--no-verify` 或强制推送。
- 提交前检查 staged 文件列表、完整 diff 与敏感信息，推送前检查完整待推送历史；发现疑似敏感信息时只报告规则和仓库相对路径，不回显命中内容。
- 详细命令见 `.codex/docs/git-workflow.md`；执行同步、创建 WIP 分支、暂存、提交、推送或准备 PR 分支时使用 `$maa-git-workflow`。
